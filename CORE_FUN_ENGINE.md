# 핵심 재미 엔진 — 4렌즈 교차검증 종합

> 2026-07-30. 방법: 서로 독립인 4개 분석(심리학 렌즈 / 메커니즘 역설계 렌즈 / 적대 렌즈 / Codex 별도 AI)에 같은 가설을 주고 "핵심 재미 엔진"을 추출시킨 뒤 교차 대조. 근거: `psychological_nudge_techniques_master.md`(§19–21), `GAME_PRINCIPLES.md`, `SOLO_GAME_MARKET_RESEARCH.md`, 포털 실측(얍 vase 86 이탈 · 2048 157판 · 행성키우기 티어명 체감 · 1라운드 3종 전멸 · merge 곰 도달불가 조사).

---

## 0. 판정: 원래 가설은 엔진이 아니라 재료 목록이었다

원래 가설 — "드래그 단순 조작 × 발라트로식 변형기 × 이름 붙은 티어 사다리" — 은 **4개 렌즈 모두에게 기각**됐다. 세 요소는 성공작에서 사후 관찰되는 표면 특징(껍데기 표현형)이고, 그 재료를 다 넣고도 재미없는 게임이 5분 만에 설계 가능하다(적대 렌즈의 사고실험). 재료가 아니라 **인과 루프**가 엔진이다.

## 1. 엔진 (4렌즈 수렴)

> **읽지 않고 이해되는 한 동작으로 판을 굴리다가, 내가 직접 고른 것이 그 동작의 "최적해"를 뒤집고, 그 인과가 즉시 눈에 보여서 "내가 조합을 알아냈다"를 느끼고, 이름 붙은 다음 문턱에 닿는지 확인하러 한 판 더 돈다.**

인과 사슬 (Codex 표현):
```
단순한 수 선택 → 결과 확인 → 변형기 선택 → 가치 구조 변화
→ 기존 전략 폐기 → 새 최적해 발견 → 증폭된 결과 → 다음 변형기 …
```

엔진의 심장은 심리 카탈로그 **§21.7 최적 재편 원칙**의 판 단위 이식이다: 새 조각은 점수를 더 주는 게 아니라 **기존 정답을 다시 열어야** 한다. 규칙(동작 한 줄)은 고정한 채 결정만 늘린다(§21.2 규칙 복잡도 ≠ 결정 깊이).

급소는 가운데다: "점수가 튄다"가 아니라 **"내가 고른 것 때문에 튀었다"**가 느껴져야 돈다 (자기효능감 §10.3 + 보상예측오차 §20). 이 인과 체감이 끊기면 어떤 레버를 붙여도 안 산다.

## 2. 필수 부품 (없으면 죽는 것)

| # | 부품 | 죽는 방식 | 근거 |
|---|---|---|---|
| 1 | **자명한 첫 동작** (3초, 학습 입장료 0) | 입장 전 이탈 | 1라운드 brew·ward·chain 전멸 |
| 2 | **정답이 자명하지 않게 만드는 제약** (공간 압박·기회비용·비가역 중 ≥1) | 판단이 아니라 작업이 됨 | §21.4. 재미의 절반은 "좁은 쪽"이다: 조커 5칸, 4×4, 슬롯 6+6 |
| 3 | **최적해를 실제로 바꾸는 선택** (제시는 랜덤, 결정은 플레이어, 효과는 곱셈) | 지배 전략 → 선택이 정렬로 퇴화 → 3판째 노동 | §21.1·21.7. "점수 +20%"는 변형기가 아니라 보상 숫자 |
| 4 | **선택→결과 인과의 즉시 판독성** (화면에 보이는 인과, 발라트로의 조커별 순차 연출이 원형) | 선택이 노이즈가 됨. 가장 흔하고 조용한 사인 | 적대 렌즈: 발라트로에서 실제로 일한 부품은 조커의 존재가 아니라 점수 연출 |
| 5 | **이름 붙은 이산 문턱 + 미탐색 잔여** (점수가 아니라 "넘었나/못 넘었나" + 다음 칸의 실루엣이 보임) | 세션이 안 끝나고 안 시작됨 / 도감 소진 = 이탈 | 2048 157판(타일=수집 대상), 행성키우기 "다중우주", §21.5 실루엣+단서 하나 |

**시간 순서도 부품이다** (6개 성공작 전부, 예외 없음):
```
0~30초: 손맛만 (변형기 0, 텍스트 0) → 첫 문턱 판정 → 그 뒤에야 첫 3택1
```
변형기를 첫 화면에 놓은 성공작은 없다. 역순이 1라운드 전멸의 사인이다.

## 3. 증폭기 (있으면 좋지만 엔진 아님 — 오인 주의)

- **티어 고유명사** ("다중우주"): 점수를 목표로 번역하는 최강 증폭기. 단, **기계적 결과의 라벨일 때만** 작동(수박게임 과일은 이름이 아니라 크기가 판을 압박해서 목표가 됨). 도달 불가능한 이름 칸은 동기가 아니라 조롱 (merge 곰 실측).
- 도감/해금 종수, 물리 무작위성, 시각적 과부하, 주스·연출, 자기 최고 기록, 스트릭·랭킹(현 유입 0에선 무의미), 웹→스팀 퍼널(유통이지 엔진 아님).

**역효과 목록**: 시간 압박·카운트다운·강제 연속(§17, GAME_PRINCIPLES §3.4 금지, 얍의 결과 정면충돌). 방치 보상(자기효능감 희석).

## 4. 렌즈가 갈린 지점 (정직하게)

1. **티어 이름의 지위**: 심리 렌즈는 필수(④), Codex·메커니즘·적대 렌즈는 증폭기. 종합: 엔진(루프)은 이름 없이도 돌지만, *이 운영자의 실측 데이터*(157판·다중우주)가 가리키는 최강 증폭기이므로 "필수에 준하는 1순위 증폭기"로 취급.
2. **긴장 모델 미결정** (적대 렌즈 단독 지적, 중요): 발라트로의 심장은 변동성+판을 잃을 위험인데, 이 포털이 검증한 결은 차분·무압박·자기기록이다. 두 리텐션 모델은 배타적이며 **어느 긴장으로 붙잡을지 결정 안 됨**. → 프로토타입에서 A/B할 1순위 설계 결정.
3. **엔진 ≠ 성공** (적대 렌즈): 같은 엔진으로 수박게임은 스트리머 픽업 후 매출 5만 배, LbaL vs 발라트로는 6배 차. 엔진은 입장권이고 유통·정체성·타이밍이 승부를 가른다. 로그라이크 덱빌더의 리뷰 1000+ 도달률 5.1%도 "기저율의 1.7배"일 뿐. → 엔진 검증과 유통 전략은 별도 트랙.

## 5. 가설의 빈칸 (Codex·적대 렌즈 합동 지적 — 다음 작업)

**"드래그로 무엇을 판단하는가"가 정의돼 있지 않다.** 변형기·티어는 구체적인데 정작 기본 판의 재미있는 한 수가 비어 있다. 기본 판이 얕으면 변형기는 같은 행동의 배율 장식일 뿐이다.

→ 프로토타입 0단계 관문: **변형기 없이, 같은 규칙만으로 10판 동안 "다른 수를 둘 이유"가 생기는가?** 아니면 발라트로식 시스템은 시체에 배율을 붙이는 작업이 된다.

## 6. 최소 사양 (메커니즘 렌즈, 실존 게임 수치 근거)

- 변형기: 동시 슬롯 4~5 / 초기 카탈로그 ~24종 / 판당 픽 6~8회. 각 변형기는 최소 2개의 다른 변형기와 곱해질 것(독립 가산 24종 = 사실상 12종). 기준선: **20판 안에 같은 빌드가 두 번 나오면 부족.**
- 변형기 표기: **아이콘 + 8자 이내 한 줄** (발라트로/LbaL 텍스트 밀도 이식 금지 — 1라운드 사인 재발 경로).
- 문턱: 판당 6~8개, 칸당 ×1.7~2.1 (3배↑=벽, 1.4배↓=긴장 없음). 1/3은 규칙을 바꾸는 제약형 문턱(보스 블라인드형).
- 사다리 두 겹: 판 내부 8~11칸(전 칸 고유명사+그림, 숫자 가려도 서열 보이게) + 판 초월 20칸 이상/실질 무한(도감·환생·티어명). 단일 11칸은 끝 보면 반드시 천장(수박게임·2048·vase 86 공통 사인).
- 판 길이: **5~12분** (웹·터치·간헐 플레이 기준. 발라트로 30~40분은 이 환경에 과함).

## 7. 검증 가능한 예측 (프로토타입 계측 신호 — 재미 판정 없이 kill/keep 가능)

- **P1. 선택 엔트로피**: 같은 변형기 제시 시 선택률 80% 초과 = 지배 전략 발현 = 엔진 실패. 같은 플레이어의 10판 빌드가 판마다 달라져야 함.
- **P2. 행동 분화** (Codex): 변형기별로 드래그 대상·위치·순서 분포가 유의하게 갈릴 것. 선택은 다른데 행동 로그가 같다면 변형기는 사후 배율 장식.
- **P3. 재시작률**: 판 종료 후 60초 내 재시작 ≥50%, 세션당 판수 분포가 이봉(1판 이탈군 vs 5판+ 몰입군). 게이트: 첫 입력까지 5초 초과면 즉시성 붕괴, 다른 지표 볼 필요 없음.
- **P4. 이탈 위치**: 세션 마지막 판 점수가 다음 문턱의 80~99% 구간에 몰리면 사다리가 심리적으로 작동 중. 무상관 산포면 이름은 장식.
- **P5. 티어명 A/B** (Codex): 숫자만 vs 이름·실루엣·남은 거리 표시 — 후자에서 문턱 직전 실패 후 즉시 재시도율이 높아야 증폭기 입증.

## 8. merge에 대한 즉각 함의 (적대 렌즈)

현 merge는 엔진의 부품 3·4·5를 위반 중이며, 가설이 참이려면 셋 다 참이어야 하는 조건이다:
1. **곰(티어10) 도달 불가** (시뮬 20판 0회, 봇 201드롭 티어8 사망, 판다 2개가 360px 보드에 물리적으로 안 들어감) — 사다리 정점이 존재하지 않음 = 조롱.
2. **아이템이 가중 랜덤 지급** (자석35/폭탄30/흔들기20/집게15) — 플레이어가 고르지 않음 = 자기효능감 부품 부재. "발라트로식 변형기를 넣었다"는 자기 진단은 오분류.
3. **아이템 효과가 곱하지 않고, 인과 연출 없음** — 조합 폭발 0, 판독성 0.

---

*작성 트랙: lens-psych(심리 레버 최소집합·죽는 조건 D1–D3·예측 P1–P3), lens-mech(6게임 역설계·4부품 루프·최소 사양), lens-skeptic(생존자 편향·재료≠엔진 사고실험·1인 밸런싱 리스크·유통 변수), Codex gpt(재최적화 루프·기본 동사 빈칸·2048/수박 반증). 원문 보고서는 세션 기록에 있음.*
